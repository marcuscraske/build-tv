- name: Copying Notification Client JAR
  copy: src={{ notification_client }}/target/notification-client-2.0.jar
        dest={{ deploy_path }}
        mode=0770
        owner=wallboard
        group=wallboard
  sudo: true
