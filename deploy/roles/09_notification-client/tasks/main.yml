- name: Copying Notification Client JAR
  copy: src={{ notification_client }}/target/notification-client-1.0.jar
        dest={{ deploy_path }}
        mode=0777
        owner=wallboard
        group=wallboard
  sudo: true
